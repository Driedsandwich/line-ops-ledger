# Runbook

## 10分で再開する手順
1. `README.md` を読む
2. Issue #2 の Context Hub を開く
3. 現在の open PR を確認する
4. ローカルで `npm install && npm run dev` を実行する
5. `/` と `/settings` の表示を確認する

## 直近の確認ポイント
- ダッシュボードで `0件 / 問題なし` が見えるか
- 設定画面で永続ストレージ状態が読めるか
- manifest / service worker が登録されているか
